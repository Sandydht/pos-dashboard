import { GenerateUppercaseSlugPipe } from './generate-uppercase-slug-pipe';

describe('GenerateUppercaseSlugPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateUppercaseSlugPipe();
    expect(pipe).toBeTruthy();
  });
});
